@model System.Int32[]

@{
    ViewBag.Title = "TestArray";
}

<h2>TestArray</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
@for (var i = 0; i < Model.Length; i++) {
    <p>@i: @Model[i]</p>
}
